<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>平均値グラフ</title>
</head>
<body>
    <h2>U列〜AK列の平均値（グラフ表示）</h2>
    <img src="data:image/png;base64,{{ img_base64 }}" />
    <h3>各列の平均値</h3>
    <ul>
        {% for i, avg in averages %}
            <li>列 {{ i }}: {{ "%.2f"|format(avg) }}</li>
        {% endfor %}
    </ul>
</body>
</html>